MacOS X dotfiles and Utilities
================

by Phil Balchin

## Installation

Use `stow` to install the dotfiles. For example:

```bash
git clone ~/.dotfiles
cd ~/.dotfiles
stow git
stow nvim
stow zsh
```

